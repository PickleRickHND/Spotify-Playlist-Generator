# Copia este archivo a `.env` en la raíz del proyecto (Create React App).
# Registra la misma Redirect URI en el dashboard de Spotify.

REACT_APP_SPOTIFY_CLIENT_ID=tu_client_id
REACT_APP_REDIRECT_URI=http://localhost:3000
